set -e

npm run pre-commit

if ! command -v pre-commit >/dev/null 2>&1; then
  echo "The pre-commit CLI is required for commits. Install it with: python3 -m pip install --user pre-commit" >&2
  exit 1
fi

pre-commit run
