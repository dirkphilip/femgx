# Viewer conformance matrix

This matrix turns the minimum trustworthy CAD/FEM viewer contract into an
auditable test map. The authoritative capability scope remains
[[requirements/product-scope|Product scope]]; this note records how the current
implementation demonstrates that the in-scope basics work and where evidence
is still incomplete.

Tests provide repeatable evidence, not a mathematical guarantee against every
browser, driver, or model. The confidence floor is strongest when a requirement
is protected at three layers: validated construction or pure invariants,
CPU-to-GPU contract tests, and real hardware-WebGPU behavior.

## Evidence levels

- **Strong** — invalid state is rejected at its owner and deterministic tests
  protect the important boundary, inverse, round-trip, or identity relations.
- **Hardware-local** — real WebGPU pixels, depth, picking, or interaction are
  covered by the required local system-Chrome lane, but not by hosted merge CI.
- **Partial** — useful targeted evidence exists, but an important state sequence
  or end-to-end relation is not yet exercised systematically.

## Minimum trustworthy viewer

| Contract                                 | Acceptance invariant                                                                                                                                                                                                                                                                        | Non-visual evidence                                                                                                                                                                                                                                                         | GPU/browser evidence                                                                                                                                                                              | Current confidence and residual gap                                                                                                                                                       |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Built-in FE geometry and generic mapping | Point, Line, Line3, Triangle, Tri6, Quad, Quad8, Tet4, Tet10, Wedge6, Pyramid5, Hex8, and Hex20 validate canonical connectivity and tessellate without losing element, face, body, or node identity. A generic multi-face solver representation maps to the same indexed-triangle contract. | [`test/elements`](../../test/elements), especially `element.test.ts` and `golden.test.ts`; [`test/geometry/element-mesh.test.ts`](../../test/geometry/element-mesh.test.ts); [`test/demo/fixture/element-fixture.test.ts`](../../test/demo/fixture/element-fixture.test.ts) | `e2e/demo-lifecycle.spec.ts` renders the helper/mapping gallery; `e2e/mobile.spec.ts` repeats it at phone size.                                                                                   | **Strong**, with hardware evidence local only.                                                                                                                                            |
| Parts, assemblies, and instancing        | Geometry uploads once per reusable part; nested and repeated assembly occurrences remain independently addressable; compile order, slots, transforms, and placement handles are deterministic.                                                                                              | [`test/scene/scene.test.ts`](../../test/scene/scene.test.ts); [`test/scene-runtime`](../../test/scene-runtime); `test/renderer/gpu-draw.test.ts` verifies geometry reuse and instanced batching.                                                                            | `e2e/webgpu-lifecycle.spec.ts` renders an instanced frame; `e2e/demo-visibility.spec.ts` exercises repeated occurrence identity.                                                                  | **Strong**, with hardware evidence local only.                                                                                                                                            |
| Camera and framing                       | Every accepted camera has finite values, a non-degenerate right-handed frame, valid projection parameters, and safe near/far coverage; orbit, pan, zoom, fit, projection changes, and transitions preserve those relationships.                                                             | [`test/camera`](../../test/camera); `test/viewport/fem-viewport.test.ts` protects camera ownership and invalid replacement; `test/renderer/gpu-frame.test.ts` protects camera-space lighting.                                                                               | `e2e/webgpu-camera.spec.ts` covers clipping, poles, full rotations, deep zoom, fit, view cube, and gestures; `e2e/mobile-touch.spec.ts` covers touch sequences.                                   | **Strong** for named CPU invariants and **Hardware-local** for rendered transitions. Add focused sequence regressions only when they protect a distinct state handoff.                    |
| Surface rendering                        | Visible triangles have correct depth, stable two-sided flat lighting, deterministic opaque output, weighted transparency, and overlays that do not corrupt the solid frame.                                                                                                                 | `test/renderer/gpu-shaders.test.ts` protects derivative normals, lighting, opacity, overlay depth, and WGSL layouts; `test/renderer/gpu-frame.test.ts` protects the fixed camera-space key direction.                                                                       | `e2e/webgpu-rendering.spec.ts` covers pixels, lighting through zoom, depth-tested edges and nodes, transparency, selection, and reload determinism.                                               | **Hardware-local**: the behavior is well covered on system Chrome, but hosted CI has no real-GPU lane. Subjective CAD appearance still requires screenshot inspection.                    |
| Readable node annotations                | Node glyph membership follows part/instance style, nearer surfaces occlude nodes, visible circles default to six CSS pixels and accept independent `[1,64]` sizing after DPR scaling, and dense overlap does not darken cumulatively.                                                       | `test/renderer/gpu-shaders.test.ts`, `gpu-draw.test.ts`, `gpu-elements.test.ts`, and `test/viewport/fem-viewport.test.ts` protect sprite sizing, depth, membership, draw order, and identities.                                                                             | `e2e/webgpu-rendering.spec.ts` covers separate node rendering, complete point sprites, orbit, and fine zoom; `e2e/mobile.spec.ts` covers DPR coordinates.                                         | **Hardware-local** because depth and blending require real raster evidence.                                                                                                               |
| Picking and engineering identity         | A visible sample maps deterministically to the correct instance, body, element, face, and node; deformation and visibility do not silently remap ids. Region picking returns unique nearest-visible targets without mutating interaction state.                                             | [`test/picking`](../../test/picking); `test/renderer/gpu-elements.test.ts`, `gpu-pick.test.ts`, `gpu-pick-region.test.ts`, and `pick-format.test.ts`; geometry tests protect authored ids.                                                                                  | `e2e/demo-interaction.spec.ts`, `webgpu-lifecycle.spec.ts`, `webgpu-rendering.spec.ts`, and `webgpu-visibility.spec.ts` cover real picks, occlusion, deformation, visibility, and region targets. | **Hardware-local** for final raster-to-id correctness; deterministic encoding and resolution are **Strong** without pixels.                                                               |
| Interaction and visibility               | Selection, hover, highlight, explicit overrides, hide/show, and body/face/node precedence are immutable and deterministic; changing one target leaves unrelated occurrences unchanged.                                                                                                      | [`test/interaction`](../../test/interaction); `test/viewport/interaction-diff.test.ts`; scene-runtime visibility and delta tests.                                                                                                                                           | `e2e/demo-interaction.spec.ts`, `demo-visibility.spec.ts`, and `webgpu-visibility.spec.ts` cover state-to-pixel and state-to-pick behavior.                                                       | **Strong**, with final visual/pick evidence hardware-local.                                                                                                                               |
| FEM results and deformation              | Authored scalar fields at nodal or elemental locations validate shape and location; scalar ranges and mapping are correct; authored nodal vectors drive deformation without losing node, element, or tessellation ownership.                                                                | [`test/results`](../../test/results); `test/viewport/results.test.ts`; `test/viewport/heterogeneous-results.test.ts`; renderer deformation tests.                                                                                                                           | `e2e/demo-results.spec.ts` covers contours, deformation, state transitions, and original pick ids; the Hex20 cylinder journey protects connected deformation.                                     | **Strong**, with rendered contours and GPU deformation hardware-local.                                                                                                                    |
| Box selection                            | The gesture produces only normalized rectangles; the frustum exposes six normalized inward planes; GPU region discovery is nearest-visible, deterministic, bounded, and side-effect free.                                                                                                   | `test/interaction/box-selection.test.ts`, `box-frustum.test.ts`, and `test/renderer/gpu-pick-region.test.ts`.                                                                                                                                                               | `e2e/demo-lifecycle.spec.ts` covers gesture lifecycle; `e2e/webgpu-lifecycle.spec.ts` covers visible region targets.                                                                              | **Strong**, with region raster evidence hardware-local.                                                                                                                                   |
| VTK and GLB input                        | Valid VTK preserves FE nodes, elements, sets, metadata, and results through the supported round trip; malformed data reports diagnostics. Bytes-only GLB imports supported display geometry and hierarchy without inventing FE semantics.                                                   | [`test/io`](../../test/io), especially `vtk.test.ts`, `roundtrip.test.ts`, `validate.test.ts`, and `glb.test.ts`.                                                                                                                                                           | `e2e/webgpu-glb.spec.ts` opens GLB on desktop and phone; demo journeys render VTK and GLB models.                                                                                                 | **Strong** for parsing and identity; rendered import evidence hardware-local.                                                                                                             |
| WebGPU platform and lifecycle            | A usable WebGPU device is the only renderer; unsupported environments return a typed reason; device loss is reported and supported-path recovery rebuilds state without stale callbacks.                                                                                                    | [`test/platform`](../../test/platform); `test/renderer/gpu-recovery.test.ts` and `gpu-renderer.test.ts`; `test/viewport/fem-viewport.test.ts`.                                                                                                                              | `e2e/webgpu-lifecycle.spec.ts` asserts a hardware adapter locally and the no-fallback unsupported contract in merge CI.                                                                           | Unsupported behavior is **Strong** in CI and successful rendering is **Hardware-local**. Recovery has strong controlled fake-device evidence but does not force real browser device loss. |
| Responsive inspection shell              | Primary controls, canvas coordinates, picking, visibility tree, menus, and view cube remain usable at desktop and 390×844 phone dimensions.                                                                                                                                                 | DOM/controller tests under [`test/demo`](../../test/demo) and orientation-gizmo tests.                                                                                                                                                                                      | `e2e/mobile.spec.ts`, `mobile-touch.spec.ts`, and desktop demo suites.                                                                                                                            | **Hardware-local** for the real canvas; responsive layout changes still require inspected screenshots.                                                                                    |

## Merge guarantee today

Every pull request must pass construction, invariant, CPU-to-GPU layout,
coverage, performance-budget, package, and unsupported-WebGPU tests in hosted
CI. Rendering, depth, picking, lighting, and responsive evidence additionally
requires `npm run test:e2e` on system Chrome with hardware WebGPU before a
rendering-related change is merged.

This is not yet one fully automated merge guarantee: hosted CI has no real-GPU
runner. A green hosted build proves the non-visual contract and the explicit
unsupported path; it does not prove that a real adapter presented correct
pixels.

## Highest-value gaps

1. **Hardware WebGPU in CI.** Run the existing `chrome` project on a real-GPU
   runner. Do not substitute SwiftShader or weaken capability assertions.
2. **Focused state-handoff regressions.** When a camera, runtime, visibility,
   deformation, recovery, or pick snapshot bug is found, protect the shortest
   operation sequence that reproduces the stale state at its owning boundary.
   Do not add broad randomized sequences or duplicate subsystem assertions.
3. **Explicit audit maintenance.** When a core requirement changes, update its
   matrix row in the same change. A row may become stronger only when its cited
   evidence protects the stated invariant.

Related: [[engineering/test-strategy|Test strategy]],
[[engineering/state-invariants|Invariant-driven state design]],
[[engineering/e2e-policy|E2E policy]], and
[[rendering/webgpu-e2e|WebGPU browser e2e lane]].

[engineering/e2e-policy|E2E policy]: e2e-policy.md
[engineering/state-invariants|Invariant-driven state design]: state-invariants.md
[engineering/test-strategy|Test strategy]: test-strategy.md
[rendering/webgpu-e2e|WebGPU browser e2e lane]: ../rendering/webgpu-e2e.md
[requirements/product-scope|Product scope]: ../requirements/product-scope.md
